<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.sns.timeline.dao.LikeDAO">
 	<select id="selectLikeList" resultType="com.sns.timeline.model.Like">
 		SELECT
 			`userId`
 			,`postId`
 			,`createdAt`
 		FROM
 			`like`
 	</select>
 </mapper>